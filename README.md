# microUtils
Microcontroller Utilities

A simple list of functions that can be used mainly with a Arduino supported microcontroller.

These are mainly testes on an AVR type (Arduino UNO, Nano) but they should work for others as they are mainly text processors of simple functions with defined pins.